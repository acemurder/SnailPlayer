<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
                name="actionHandler"
                type="com.ride.snailplayer.framework.ui.search.SearchActivity"/>

    </data>

    <FrameLayout
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="@{actionHandler.doExitAnim}"
            tools:context="com.ride.snailplayer.framework.ui.search.SearchActivity">

        <!-- we use this scrim view rather than a semi-opaque window background so that we can
         overlap it's enter/exit animation with content animations i.e. circular reveal/hide
         of the search panel on v21+. -->
        <View
                android:id="@+id/scrim"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#7f000000"
                android:alpha="0" />

        <android.support.v7.widget.CardView
                android:id="@+id/card_search_panel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/padding_normal"
                app:cardBackgroundColor="@color/card"
                app:cardCornerRadius="@dimen/card_corner_radius"
                app:cardElevation="@dimen/card_elevation">

            <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                <android.support.v7.widget.Toolbar
                        android:id="@+id/toolbar"
                        android:layout_width="match_parent"
                        android:layout_height="?attr/actionBarSize"
                        android:gravity="center_vertical">

                    <android.support.v7.widget.SearchView
                            android:id="@+id/search_view"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:imeOptions="actionSearch|flagNoExtractUi" />

                </android.support.v7.widget.Toolbar>

                <View
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/divider_height"
                        android:background="@color/divider" />

                <android.support.v7.widget.RecyclerView
                        android:id="@+id/rv_search_results"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingTop="@dimen/spacing_normal"
                        android:paddingBottom="@dimen/spacing_normal"
                        android:clipToPadding="false"
                        android:divider="@null"
                        android:visibility="gone" />

            </LinearLayout>

        </android.support.v7.widget.CardView>

    </FrameLayout>

</layout>

